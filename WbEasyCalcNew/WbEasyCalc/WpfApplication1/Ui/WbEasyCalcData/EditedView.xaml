<UserControl x:Class="WpfApplication1.Ui.WbEasyCalcData.EditedView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"             
             mc:Ignorable="d" 
             Height="100" Width="1000"
             >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50"   />
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="300" />
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="150" />
            <ColumnDefinition Width="100" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Margin="7 7 7 0" Text="Id" Grid.Column="0" Grid.Row="0" VerticalAlignment="Center"/>
        <TextBlock Margin="7 7 7 0" Text="ZoneId" Grid.Column="1" Grid.Row="0" VerticalAlignment="Center"/>
        <TextBlock Margin="7 7 7 0" Text="YearNo" Grid.Column="2" Grid.Row="0" VerticalAlignment="Center"/>
        <TextBlock Margin="7 7 7 0" Text="MonthNo" Grid.Column="3" Grid.Row="0" VerticalAlignment="Center"/>
        <TextBlock Margin="7 7 7 0" Text="Description" Grid.Column="4" Grid.Row="0" VerticalAlignment="Center"/>
        <TextBlock Margin="7 7 7 0" Text="Start_PeriodDays_M21" Grid.Column="5" Grid.Row="0" VerticalAlignment="Center"/>

        <TextBox Margin="7" Grid.Column="0" Grid.Row="1" Text="{Binding Path=Model.Id, UpdateSourceTrigger=PropertyChanged}" IsEnabled="False" />
        <TextBox Margin="7" Grid.Column="1" Grid.Row="1" Text="{Binding Path=Model.ZoneId, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBox Margin="7" Grid.Column="2" Grid.Row="1" Text="{Binding Path=Model.YearNo, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBox Margin="7" Grid.Column="3" Grid.Row="1" Text="{Binding Path=Model.MonthNo, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBox Margin="7" Grid.Column="4" Grid.Row="1" Text="{Binding Path=Model.Description, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBox Margin="7" Grid.Column="5" Grid.Row="1" Text="{Binding Path=Model.Start_PeriodDays_M21, UpdateSourceTrigger=PropertyChanged}"/>
    </Grid>
</UserControl>
